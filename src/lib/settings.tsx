import type { Envelope } from "../openAir/openAirTypes";

export const envelopeDefaults: Envelope = { floor: 0, ceiling: 1000 };
export const modelScale = 0.1
export const drawerWidth = 320
export  const topBarHeight = 80
export const logoBarHeight = 140